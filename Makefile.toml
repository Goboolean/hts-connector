[tasks.test]
command = "cargo"
env = { RUSTFLAGS = "-A dead_code" }
args = [
    "test",
    "--all-targets",
    "--",
]

[tasks.test-sensitive]
command = "cargo"
env = { RUSTFLAGS = "-A dead_code" }
args = [
    "test",
    "--all-targets",
    "--",
    "--ignored",
    "--test-threads=1"
]

[tasks.test-debug]
command = "cargo"
env = { RUSTFLAGS = "-A dead_code" }
args = [
    "test",
    "--all-targets",
    "--",
    "--nocapture"
]

[tasks.build]
command = "cargo"
env = { RUSTFLAGS = "-A dead_code" }
args = [
    "build",
]

[tasks.lint]
command = "cargo"
args = [
    "clippy",
    "--all-targets",
    "--all-features",
    "--",
    "-D", "warnings",
    "-A", "dead_code"
]
